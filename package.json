{
  "name": "ledger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Personal technical blog — Astro static site, built to the Ledger design system.",
  "engines": {
    "node": "24.x",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:astro": "astro check",
    "check:css": "stylelint \"src/**/*.css\" \"src/**/*.astro\"",
    "check:format": "prettier --check .",
    "check:fonts": "node scripts/verify-fonts.mjs",
    "format": "prettier --write .",
    "check:content": "echo 'TODO Phase 5 — content invariant tests (tests/invariants)' && exit 0",
    "check:links": "echo 'TODO Phase 8 — lychee link check on build output' && exit 0",
    "verify": "pnpm run check:format && pnpm run check:astro && pnpm run check:css && pnpm run check:fonts && pnpm run check:content"
  },
  "dependencies": {
    "astro": "^7.2.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "postcss-html": "^2.0.0",
    "prettier": "^3.3.3",
    "prettier-plugin-astro": "^0.14.1",
    "stylelint": "^17.14.1",
    "typescript": "^6.0.3"
  }
}
