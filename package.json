{
  "name": "ledger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Personal technical blog — Astro static site, built to the Ledger design system.",
  "engines": {
    "node": "24.x",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check:svelte": "astro check && svelte-check --tsconfig ./tsconfig.json",
    "check:astro": "astro check",
    "check:css": "stylelint \"src/**/*.css\" \"src/**/*.astro\"",
    "check:format": "prettier --check .",
    "check:fonts": "node scripts/verify-fonts.mjs",
    "check:tokens": "node scripts/verify-tokens.mjs",
    "check:content": "echo 'TODO Phase 5 — content invariant tests (tests/invariants)' && exit 0",
    "check:e2e": "astro dev --background && playwright test",
    "check:links": "echo 'TODO Phase 8 — lychee link check on build output' && exit 0",
    "format": "prettier --write .",
    "verify": "pnpm run check:format && pnpm run check:astro && pnpm run check:css && pnpm run check:tokens && pnpm run check:fonts && pnpm run check:content && pnpm run check:e2e && pnpm run check:links && pnpm run check:svelte"
  },
  "dependencies": {
    "@astrojs/svelte": "^9.0.1",
    "astro": "^7.2.6",
    "svelte": "^5.57.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.4.1",
    "postcss-html": "^2.0.0",
    "prettier": "^3.3.3",
    "prettier-plugin-astro": "^0.14.1",
    "stylelint": "^17.14.1",
    "svelte-check": "^4.7.6",
    "typescript": "^6.0.3"
  }
}
